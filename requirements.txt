# deepin-agent-teams 依赖
#
# 核心依赖
erniebot==0.5.9
python-dotenv==1.2.2
tiktoken>=0.5.0

# GUI 依赖
PyQt5==5.15.11

# OCR 依赖（可选，用于屏幕文字识别）
# paddlepaddle
# paddleocr

# 系统工具（可选，用于环境感知）
# python-xlib          # X11 窗口管理
# dbus-python          # D-Bus 通信

# MCP 协议
# 注意：mcp_servers/mcp_protocol.py 是纯 Python 实现，不需要 mcp SDK
# 如果需要使用官方 MCP SDK，取消下面的注释：
# mcp>=1.0.0
